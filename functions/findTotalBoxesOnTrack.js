function findTotalBoxesOnTrack(arr1, arr2){
    if(isPaused){

    } else {
        totalBoxesOnTrack = 0;
        let i = 0;
        let b = 0;
        arr1.forEach((package)=>{
            i+=1;
        })
        arr2.forEach((package)=>{
       b +=1;
        })
        totalBoxesOnTrack = i + b;
    }
    return totalBoxesOnTrack;
    }

    